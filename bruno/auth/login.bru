meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: http://localhost:3099/auth/login
  body: json
}

body:json {
  {
    "username": "bruno",
    "password": "password123"
  }
}

script {
  const jsonData = res.getBody();
  if(jsonData && jsonData.access_token) {
    bru.setVar('jwt_token', jsonData.access_token);
  }
}
