meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{host}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "identification": "admin@admin.com",
    "password": "12345678!"
  }
}

script:post-response {
  const token = res.getBody().token
}
