<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Alerta de Segurança</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .container {
            background-color: #fff5f5;
            padding: 20px;
            border-radius: 10px;
            border-left: 5px solid #dc3545;
        }
        .alert-header {
            background-color: #dc3545;
            color: white;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            text-align: center;
        }
        .alert-details {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .button {
            display: inline-block;
            background-color: #dc3545;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
        }
        .button:hover {
            background-color: #c82333;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 15px;
        }
        .timestamp {
            font-weight: bold;
            color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="alert-header">
            <h1>🚨 {{alertType}}</h1>
        </div>
        
        <p>Olá, <strong>{{name}}</strong>!</p>
        
        <p>{{message}}</p>
        
        <div class="alert-details">
            <h3>Detalhes do Alerta:</h3>
            <p><strong>Tipo:</strong> {{alertType}}</p>
            <p><strong>Data/Hora:</strong> <span class="timestamp">{{timestamp}}</span></p>
            {{#if ip}}
            <p><strong>Endereço IP:</strong> {{ip}}</p>
            {{/if}}
            {{#if userAgent}}
            <p><strong>Navegador:</strong> {{userAgent}}</p>
            {{/if}}
            {{#if attemptCount}}
            <p><strong>Número de Tentativas:</strong> {{attemptCount}}</p>
            {{/if}}
            {{#if blockDuration}}
            <p><strong>Duração do Bloqueio:</strong> {{blockDuration}}</p>
            {{/if}}
        </div>
        
        <h3>O que fazer agora?</h3>
        <ul>
            <li>Se você reconhece esta atividade, pode ignorar este email</li>
            <li>Se você <strong>não</strong> reconhece esta atividade:
                <ul>
                    <li>Altere sua senha imediatamente</li>
                    <li>Verifique se há atividades suspeitas em sua conta</li>
                    <li>Entre em contato conosco se precisar de ajuda</li>
                </ul>
            </li>
        </ul>
        
        <div class="footer">
            <p><strong>Importante:</strong> Este é um email automático de segurança. Não responda a este email.</p>
            <p>Se você não solicitou esta ação ou tem dúvidas sobre a segurança de sua conta, entre em contato conosco imediatamente.</p>
            <p>© 2024 - Sistema de Segurança Automatizado</p>
        </div>
    </div>
</body>
</html>